 
import tensorflow as tf
from tensorflow.keras.models import Sequential we.
from tensorflow.keras.layers import Densel, Dropouti, [LSTM |#Cu@AK—.

 
mnist = tf.keras.datasets.mnist
(x_train, y_train)|,(x_test, y_test) = mnist.load_data()

print(x_train. shape)
print(x_train)